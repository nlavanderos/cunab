<%= form_with(model: foro, local: true) do |form| %>
  <% if foro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(foro.errors.count, "error") %> prohibited this foro from being saved:</h2>

      <ul>
        <% foro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :anuncio %>
    <%= form.text_field :anuncio %>
  </div>

  <div class="field">
    <%= form.label :foro %>
    <%= form.text_field :foro %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
