<%= form_with(model: curso_seccion, local: true) do |form| %>
  <% if curso_seccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curso_seccion.errors.count, "error") %> prohibited this curso_seccion from being saved:</h2>

      <ul>
        <% curso_seccion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.number_field :curso_id %>
  </div>

  <div class="field">
    <%= form.label :seccion_id %>
    <%= form.number_field :seccion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
