<%= form_with(model: seccion, local: true) do |form| %>
  <% if seccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seccion.errors.count, "error") %> prohibited this seccion from being saved:</h2>

      <ul>
        <% seccion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :seccion %>
    <%= form.number_field :seccion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
